(defn nth [seq n]
  (defn iter [seq k]
    (if (= k n)
      (first seq)
      (iter (rest seq) (+ k 1))))
  (iter seq 0))


(defn nth [seq n]
  (let [g (fn [seq k]
            (if (= k n)
              (first seq)
              (g (rest seq) (+ k 1))))]
    (g seq 0)))
            